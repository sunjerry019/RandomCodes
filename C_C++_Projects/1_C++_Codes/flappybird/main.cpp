#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n, len;
    scanf("%d", &n);
    int scores[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d", &scores[i]);
        if(!i) len++;
        else if(scores[i])
    }
    return 0;
}
